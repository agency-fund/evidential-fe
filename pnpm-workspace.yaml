strictPeerDependencies: true
saveExact: true
# min age is in minutes for pnpm; 5d=7200
minimumReleaseAge: 7200
# Admitted before aging out of minimumReleaseAge; drop entries once they are older than the minimum.
minimumReleaseAgeExclude:
- '@types/node@26.4.1'
publicHoistPattern:
- "*import-in-the-middle*"
- "*require-in-the-middle*"

allowBuilds:
  '@biomejs/biome': true
  '@sentry/cli': true
  esbuild: true
  sharp: true

updateNotifier: false

overrides:
  # Orval pins a vulnerable js-yaml; remove after its dependency is patched.
  'js-yaml@>=4 <4.3.1': 4.3.1
  # Orval's Spectral toolchain resolves vulnerable lodash versions.
  'lodash@<=4.17.23': 4.18.1
